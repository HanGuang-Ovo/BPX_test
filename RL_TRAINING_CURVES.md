# BPX 训练曲线解读指南(RL / TensorBoard)

本文档汇总"如何看懂 BPX 平地行走训练的 TensorBoard 曲线",包括各条曲线的含义、PPO 曲线为什么长这样、奖励曲线"N 字形"的成因,以及判断训练健康与否的方法。环境配置见 [BPX_ENV_SETUP.md](BPX_ENV_SETUP.md)。

---

## 1. TensorBoard 基本操作

```bash
conda activate isaaclab
cd /home/hanguang/BPX_test
tensorboard --logdir logs/rsl_rl/bpx_flat      # 所有 run 叠加对比
tensorboard --logdir logs/rsl_rl/bpx_flat/2026-09-08_15-30-00   # 只看某一次
```

浏览器打开 <http://localhost:6006>,可与训练同时运行、实时刷新(每 24 步一个数据点)。

**两个必会操作:**

- **Smoothing 滑条**(左侧,默认 0.6):PPO 曲线噪声极大,拖到 **0.9~0.99** 滤掉震荡看趋势;
- **面板左上角标题**:每张图的 tag 名,先确认看的是哪条曲线再解读。

---

## 2. 曲线速查表:哪条看什么

| 曲线(tag) | 含义 | 什么时候看 | 好的样子 |
|---|---|---|---|
| `Episode Rewards/total` | 每回合奖励总和 | 主指标 | 整体趋势向上(允许中期凹陷,见 §4) |
| `Episode Rewards/track_lin_vel_xy_exp` | 线速度跟踪分 | 判断"学会走路" | 上升并趋平 |
| `Episode Rewards/track_ang_vel_z_exp` | 偏航跟踪分 | 判断"学会转弯" | 上升并趋平 |
| `Train/Mean episode length` | 平均回合步数 | 判断"还摔不摔" | 逼近 **1000**(= 20 s × 50 Hz 满回合) |
| `Policy/mean_noise_std` | 探索噪声幅度 | 判断收敛 | 从 1.0 缓慢下降 |
| `Loss/value_function` | critic 的 MSE | 只防异常 | 前期大、震荡正常;**持续爆炸才是问题** |
| `Loss/surrogate` | PPO 策略损失 | **基本不用看** | 无诊断价值,围绕 0 波动 |

> 记忆口诀:**看奖励和回合长度判断"学没学会",看 loss 只判断"崩没崩"。**

---

## 3. 两条 Loss 曲线为什么剧烈震荡

PPO 的 loss 曲线和监督学习"平滑下降"完全不同,又抖又乱是**正常形态**,原因有三:

1. **样本噪声大**:每个数据点只是一次迭代里几个 minibatch 的平均;
2. **目标在移动**:策略每轮都变,critic 追的是一个移动靶,两者互相追逐,loss 天然震荡;
3. **学习率自适应**(`schedule="adaptive"`,按 KL 调整):会周期性放大缩小,曲线出现台阶式跳变。

- `Loss/value_function`:价值网络预测"未来累计回报"的误差。数值大 = critic 还没学准;若**涨到初始的几十倍**,才是学习率过大或奖励尺度异常的信号。
- `Loss/surrogate`:带 clip 的策略目标,在 0 附近波动,PPO 论文和实践都不用它判断收敛。

**结论:不要从这两条曲线读收敛信息。**

---

## 4. 奖励曲线的"N 字形":先上升 → 中段回落 → 再上升

这是四足 locomotion 训练最典型、也最容易被误判为"训练崩了"的形状,通常是**好信号**。

### 4.1 三段分别对应什么

| 阶段 | 曲线 | 机器人在干什么 |
|---|---|---|
| ① 上升 | 全体奖励抬升 | **学会站稳不摔**:摔得少 → 回合变长 → 每回合奖励总和变大 |
| ② 回落 | 集体凹陷 | **开始学迈步**:动作幅度变大、摔得又多了、惩罚项(`action_rate`/`dof_acc`/`undesired_contacts`)变大、回合变短 |
| ③ 再上升 | 创新高 | **步态成型**:协调性建立、摔倒骤减、跟踪变准、惩罚回落 |

### 4.2 用本环境的真实数字理解

每回合 20 s × 50 Hz = 1000 步:

| 行为 | 每步主要收入 | 回合长度 | 回合总回报(量级) |
|---|---|---|---|
| 站稳但不动(指令非零) | tracking ≈ exp(−\|v_cmd\|²/0.25) ≈ 0.1~0.4 | 满 1000 步 | **≈ 100~400** |
| 会走(跟住指令) | tracking ≈ 1.0 + 0.5,接近满分 | 满 1000 步 | **≈ 1500** |
| 正在学步(迈两步就摔) | tracking 低;摔倒即终止,**后续奖励全部归零** | 100~400 步 | **≈ 20~100** |

关键在第三行:**"半站立半迈步"的混合策略,回报比纯站立、纯行走都低**——这正是中段凹陷的来源。

### 4.3 为什么多条曲线齐涨齐跌

这些曲线都按**回合求和**,共享同一个"回合长度"因子:摔得多的阶段,所有正向奖励项一起缩水、负向惩罚项一起变浅。单独看某条的绝对值意义不大,看**趋势**和它与 `total` 的**占比**。

---

## 5. 深层原理:为什么不是单调提升

### 5.1 学习目标从未改变——"阶段"是涌现的

奖励函数(11 项加权组合)和 PPO 损失从第 0 轮到最后一轮**一模一样**。"先学站再学走"不是设计出来的课程,而是三样东西在变:

1. **状态分布在变(隐式课程)**:初期策略乱动、几步就摔,数据里只有"站不稳→摔倒"的状态,"迈步拿跟踪分"根本不可达;碰巧站稳后,数据里才大量出现"四脚着地"的状态,迈步第一次成为可优化项。
   > 数据分布决定当前能学什么 → 学会的行为改变数据分布 → 解锁下一层可学的东西。
2. **各奖励项的边际收益在变**:摔倒频繁时,终止损失主导梯度;站稳后,`track_lin_vel` 主导;会走后,`feet_air_time` 和各项惩罚开始精修。**哪个项在"拖后腿",哪个项就在主导学习。**
3. **探索幅度在收缩**:`Policy/mean_noise_std` 从 1.0 逐渐下降,前期大噪声撞行为,后期小噪声做精修。

### 5.2 站立是局部最优,行走是更高的山,两峰之间隔着谷底

策略参数是**连续变化**的。从"站立参数区域"走向"行走参数区域",必须穿过一个**两边都不是**的中间区域,而中间区域的回报比站立还低。梯度上升只能翻谷,不能瞬移——训练曲线的谷底,正是"概率质量搬家"的那段时间。

### 5.3 PPO 优化的是"相对优势",不是"绝对绩效"

站稳之后,回合里充满"指令要求前进"的状态。样本间比较:**抬腿前移的轨迹比原地不动的回报高**(哪怕前者偶尔摔),于是"抬腿"拿到正优势、被强化。概率质量逐渐从站立模式挪向迈步模式——**挪的过程中绩效先恶化**;挪够了,协调性建立,回报越过谷底冲上更高的峰。

> 学习在进行、绩效在下降,两者不矛盾:梯度跟着局部优势差走,不保证全局单调。

### 5.4 凹陷不是必然的

换个随机种子或奖励塑形不同(比如 `feet_air_time` 权重大到学步早期就有正反馈),曲线可能单调上升。凹陷是**逃离局部最优的代价**,不是训练的固定节目。

---

## 6. 健康与异常信号判别

| 特征 | 判断 | 处理 |
|---|---|---|
| 中段凹陷后创新高 | ✅ 正常学习过程 | 不用管 |
| 凹陷后回不到前高、长期阴跌 | ⚠️ 可能奖励冲突或学习率问题 | 降低 `learning_rate`;检查惩罚权重是否过强 |
| 曲线断崖掉到接近 0 并保持 | ❌ 策略崩了 | 常见原因:惩罚过强、KL 失控;换 seed 重训或回退到较早 checkpoint `--resume` |
| `Loss/value_function` 持续爆炸(几十倍) | ⚠️ | 检查奖励尺度、降低学习率 |
| 回合长度一直上不去(< 200 步) | ⚠️ 站立都没学会 | 收紧指令范围先练站立(见 §8) |

---

## 7. 动手验证:回放不同 checkpoint 看行为

曲线存疑时,最直观的办法是**回放对应轮次的模型对比**:

```bash
# 回放谷底附近(假设凹陷在 400 轮)
python scripts/rsl_rl/play.py --task BPX-Test-v0 --num_envs 1 --real-time --checkpoint model_400.pt

# 指定某次训练
python scripts/rsl_rl/play.py --task BPX-Test-v0 --num_envs 1 --real-time \
    --load_run 2026-09-08_15-30-00 --checkpoint model_400.pt
```

典型观察:400 轮时迈步笨拙、时不时摔;最终模型步态流畅——这就是"N 字形"曲线背后的行为差异。

> **checkpoint 保存机制**:rsl-rl 只在 `迭代数 % save_interval(=50) == 0` 时保存,`max_iterations=1500` 时最后落盘的是 **model_1450.pt**(第 1450 轮),最后 49 轮的更新不在文件里——通常可忽略。想要严格的最终模型,把 `save_interval` 改小,或在 `train.py` 的 `runner.learn(...)` 后加 `runner.save(os.path.join(log_dir, "model_final.pt"))`。

> **回放时的可视化箭头**:机器人上方 0.5 m 处有一对箭头——**绿色 = 期望速度指令**,**蓝色 = 当前实际速度**,长度与速度成正比。策略学得好时蓝箭头紧跟绿箭头。嫌干扰可把 `CommandsCfg` 里 `debug_vis=True` 改为 `False`。

---

## 8. 曲线不好时的干预手段(课程学习)

如果凹陷太深、策略卡在谷底(摔太狠学不会迈步),人为分阶段训练——相当于给策略修一条绕过谷底的盘山路:

1. **指令范围课程**:先收紧 `CommandsCfg.ranges`(如 `lin_vel_x=(-0.2, 0.2)`)只练站立,收敛后再逐步放大到 `(-1, 1)`;
2. **提高学步期正反馈**:`feet_air_time` 权重 0.5→1.0,或 `threshold` 0.4→0.3,让"抬脚"更早拿到奖励;
3. **减小探索噪声初值**:`init_noise_std` 1.0→0.8,减少学步期的无谓摔倒。

每步只改一个变量,改完对比 Smoothing 0.9 后的趋势再决定下一步。
