# BPX 四足机器人强化学习环境改进路线图

本文档基于当前 BPX 平地速度跟踪环境，整理从“仿真中稳定行走”到“真实机器人部署”，再到“复杂地形运动”的后续改进方向。文中所述优先级以降低调试成本、保持实验可解释性和提高 Sim-to-Real 成功率为原则。

## 1. 当前系统基线

当前任务采用 Isaac Lab 的 Manager-Based RL 工作流，将场景、指令、动作、观测、事件、奖励和终止条件分别配置。该模块化方式也是 Isaac Lab 官方推荐的任务组织方式 [[1]](#ref-1)。

相关代码：

- [公共环境配置](source/BPX_test/BPX_test/tasks/manager_based/bpx_test/bpx_base_env_cfg.py)
- [行走任务配置](source/BPX_test/BPX_test/tasks/manager_based/bpx_test/bpx_locomotion_env_cfg.py)
- [PPO 配置](source/BPX_test/BPX_test/tasks/manager_based/bpx_test/agents/rsl_rl_ppo_cfg.py)
- [本地 MDP 函数](source/BPX_test/BPX_test/tasks/manager_based/bpx_test/mdp/)
- [机器人模型](source/BPX_test/BPX_test/BPX_structure/)

### 1.1 机器人与控制

| 项目 | 当前配置 |
|---|---|
| 机器人 | BPX 四足机器人，12 个可动关节 |
| 默认姿态 | 髋横滚 `0`、髋俯仰 `0.7 rad`、膝关节 `-1.4 rad` |
| 生成高度 | 机身中心 `0.40 m` |
| 动作 | 所有关节的目标位置偏移 |
| 动作映射 | `目标角度 = 默认角度 + 0.5 × action` |
| 执行器 | 隐式 PD，`Kp=40`、`Kd=1` |
| 力矩/速度限制 | `30 N·m`、`20 rad/s` |
| 物理频率 | 200 Hz，`sim.dt=0.005 s` |
| 策略频率 | 50 Hz，`decimation=4` |

### 1.2 任务与 PPO

| 项目 | 当前配置 |
|---|---|
| 任务 | 平地基座速度跟踪 |
| 指令 | `[vx, vy, wz]`，即前后速度、横向速度、偏航角速度 |
| 指令范围 | `vx∈[-1,1] m/s`、`vy∈[-0.5,0.5] m/s`、`wz∈[-1,1] rad/s` |
| 重采样周期 | 固定 10 s |
| 站立指令比例 | 2% |
| 并行环境 | 2048 |
| 回合长度 | 20 s，即 1000 个策略步 |
| 算法 | RSL-RL PPO；PPO 原理见 [[2]](#ref-2)，当前训练库见 [[10]](#ref-10) |
| 网络 | actor/critic 均为三层 `[128,128,128]` ELU MLP |

### 1.3 Actor 观测

当前只有 `policy` 观测组，actor 和 critic 共用同一套观测：

| 观测 | 维度 | 真机来源 |
|---|---:|---|
| 基座线速度 | 3 | 状态估计器或腿式里程计 |
| 基座角速度 | 3 | IMU |
| 投影重力 | 3 | IMU 姿态估计 |
| 速度指令 `[vx,vy,wz]` | 3 | 遥控器或上层规划器 |
| 相对关节位置 | 12 | 关节编码器 |
| 关节速度 | 12 | 关节编码器/差分估计 |
| 上一帧动作 | 12 | 控制程序内部记录 |

在 12 个动作关节的前提下，总观测维度为 48。Isaac Lab 会自动推断维度，无需手动设置网络输入维数。

### 1.4 当前奖励与随机化

主要正奖励为平面线速度跟踪、偏航速度跟踪和足端腾空时间；主要惩罚包括竖直速度、横滚/俯仰角速度、机身倾斜、关节力矩、关节加速度、动作变化、关节偏离及非足端接触。

当前已包含以下鲁棒性训练：

- 机器人刚体摩擦系数随机化；
- 执行器刚度和阻尼随机化；
- 初始基座状态随机化；
- 初始关节角度和速度随机化；
- 训练期间的周期性水平推扰。

这些措施已经构成基础域随机化，但尚未覆盖质量、质心、惯量、执行延迟、传感器偏置和真实电机动态等主要现实差异。系统辨识、执行器建模、延迟建模和动力学随机化是经典四足机器人 Sim-to-Real 工作中的核心环节 [[4]](#ref-4)、[[5]](#ref-5)。

## 2. 总体改进原则

推荐按照以下顺序推进：

```text
模型与接口正确性
        ↓
简单条件下稳定站立和前后行走
        ↓
完整平地速度跟踪
        ↓
观测与真机接口对齐
        ↓
逐步加入域随机化
        ↓
真机低风险测试
        ↓
复杂地形与高级步态
```

不要同时修改大量奖励、观测和随机化参数。每次实验尽量只改变一类因素，并保留可复现的基线 checkpoint。

## 3. 第一阶段：验证机器人模型与控制接口

这是最高优先级。模型或关节映射错误时，继续调整 PPO 和奖励通常没有意义。

### 3.1 必查项目

- 检查 12 个关节的名称、顺序、零位和正负方向；
- 检查默认姿态是否对称，足端是否接近地面且没有穿透；
- 检查足端正则 `.*_toe_link` 是否恰好匹配四个足端；
- 检查 `torso` 接触是否能够触发摔倒终止；
- 检查质量、质心、惯量、关节限位和碰撞体是否符合真实机器人；
- 检查 PD 增益、力矩上限和速度上限是否符合真实驱动器；
- 检查关节目标下发顺序与真机 SDK 的顺序完全一致；
- 检查自碰撞是否必要，以及是否存在错误的腿部自碰撞。

### 3.2 建议验收标准

- 零动作下机器人能够从默认姿态正常落地，不出现爆炸、穿透或 NaN；
- 单关节小幅正负动作与预期方向一致；
- 足端接触传感器只在对应足端接地时响应；
- 正常站立不会触发非法接触终止；
- 关节力矩和速度不会长期饱和。

## 4. 第二阶段：建立简单、可解释的训练基线

大规模并行 PPO 和课程学习已经被证明适合快速训练四足机器人运动策略 [[3]](#ref-3)。对 BPX 来说，建议先减少任务难度，再逐步扩展。

### 4.1 建议的初始课程

1. 零速度站立；
2. 仅训练低速前后运动，例如 `vx∈[-0.5,0.5] m/s`；
3. 加入偏航角速度；
4. 加入横向速度；
5. 扩大到当前完整指令范围；
6. 最后再逐步加入推扰和完整域随机化。

如果初期频繁摔倒，可暂时缩小：

- 基座初始速度扰动；
- 关节初始角度偏移；
- 随机推动幅度；
- 指令速度范围。

训练稳定后再恢复或扩大这些范围。

### 4.2 站立训练比例

当前 `rel_standing_envs=0.02`，只有约 2% 的环境接收零速度指令。如果真机需要可靠地响应停车指令，可在早期训练中提高到约 10%～20%，再通过实验选择最终比例。

### 4.3 训练和回放配置分离

建议建立独立的训练配置与回放配置：

- 训练：关闭 `debug_vis`，使用大量环境；
- 回放：使用 1～32 个环境，打开速度箭头和传感器可视化；
- 评估：关闭训练噪声，固定随机种子，使用预设测试集合。

## 5. 第三阶段：动作安全与执行器一致性

### 5.1 显式动作裁剪

当前 `JointPositionActionCfg` 使用 `scale=0.5`，但 RSL-RL runner 未显式配置动作裁剪。建议明确决定是否设置：

```python
clip_actions = 1.0
```

这样策略目标通常被限制在默认角度附近约 `±0.5 rad`。训练、回放和真机部署必须使用完全相同的裁剪、缩放及关节顺序。

### 5.2 匹配真实控制链路

需要测量或确认：

- 策略命令发送频率；
- 电机底层 PD 更新频率；
- 通信和计算延迟；
- 电机死区、摩擦、饱和及温度变化；
- 关节目标插值方式；
- 急停、通信超时和失控保护机制。

隐式理想 PD 与真实执行器动态存在差异。Hwangbo 等人的工作表明，执行器模型质量对高动态运动的迁移尤其重要 [[5]](#ref-5)。

## 6. 第四阶段：奖励函数与步态质量

### 6.1 当前最值得监控的风险

`feet_air_time` 当前权重为 `0.5`，明显高于所参考官方配置中的 `0.125`。如果回放中出现四脚同时腾空、持续蹦跳、步频过低或落地冲击过大，应优先检查这一项，而不是继续增加其权重。

### 6.2 建议增加的奖励或指标

- 关节位置限位惩罚；
- 支撑相足端水平滑动惩罚；
- 零指令下的站姿保持；
- 足端离地高度或绊脚惩罚；
- 足端落地冲击统计；
- 左右/对角腿相位关系；
- 单位运输能耗（Cost of Transport）；
- 动作、速度和力矩饱和比例。

如果目标是显式控制踏频、占空比、抬脚高度或不同步态，可参考 Walk These Ways 的 gait-conditioned policy 思路 [[9]](#ref-9)，而不是只依赖腾空时间奖励让步态自由涌现。

### 6.3 奖励调参原则

- 首先确认任务奖励能够上升、平均回合长度能够接近上限；
- 再处理滑脚、抖动、蹦跳和能耗；
- 每次只调整一到两个权重；
- 同时观察未加权原始量和加权奖励，避免被权重掩盖；
- 不以“总奖励最高”作为唯一标准，必须结合视频和物理指标。

## 7. 第五阶段：观测与真机可用性对齐

### 7.1 优先处理基座线速度

当前 actor 直接接收仿真真值 `base_lin_vel`。真实机器人通常不能直接测得这一量，需要状态估计器、腿式里程计或外部定位系统。

可选择以下路线之一：

1. 真机提供可靠的基座线速度估计，并在仿真中模拟相应噪声和延迟；
2. actor 不读取基座线速度，改用多帧本体感知历史进行隐式估计；
3. 将精确基座线速度只提供给 critic，actor 仅使用真机可获得的观测；
4. 联合训练状态估计网络和控制策略。相关方法可参考 Ji 等人的工作 [[6]](#ref-6)。

### 7.2 历史观测与循环网络

如果不直接给 actor 基座线速度，仅堆叠历史动作通常不够。更有价值的是堆叠若干帧完整本体感知信息：

- IMU 角速度与投影重力；
- 关节位置和关节速度；
- 速度指令；
- 历史动作。

另一种方案是使用 GRU/LSTM。历史观测或循环网络可以帮助策略推断接触状态、速度、延迟和隐含动力学。

### 7.3 非对称 Actor-Critic

可以增加独立的 `critic` 观测组：

```text
Actor：只接收真机能够获得的观测
Critic：额外接收精确速度、接触力、质量、摩擦、地形高度等仿真真值
```

这种方式能在训练阶段利用特权信息，但部署时只运行 actor。需要注意，放在 `PolicyCfg` 中的信息仍然是 actor 的输入；PPO 不会自动消除 actor 对该信息的依赖。

Lee 等人的复杂地形工作展示了仅依靠本体感知反馈获得强鲁棒性的路线 [[7]](#ref-7)；如果需要在线适应载荷、磨损和地面变化，可进一步参考 RMA [[8]](#ref-8)。

### 7.4 归一化与缩放

当前 `empirical_normalization=False`。后续应明确选择：

- 使用 RSL-RL 的经验归一化，并保证导出和部署时携带相同统计量；或者
- 为各观测项显式设置固定缩放，使其数值大致处于相近范围。

不能只在训练端归一化而在真机端输入原始量。

## 8. 第六阶段：系统化 Sim-to-Real

Tan 等人和 Hwangbo 等人的研究都强调了系统辨识、执行器建模、延迟建模和域随机化的组合价值 [[4]](#ref-4)、[[5]](#ref-5)。

### 8.1 建议逐项增加的随机化

| 类别 | 建议随机化内容 |
|---|---|
| 刚体参数 | 机身/腿部质量、质心位置、转动惯量 |
| 执行器 | 电机强度、PD 增益、最大力矩、关节摩擦、死区 |
| 时间因素 | 动作延迟、观测延迟、控制周期抖动、随机丢帧 |
| 传感器 | 编码器零偏、速度噪声、IMU 偏置和漂移 |
| 接触 | 地面摩擦、恢复系数、小坡度和轻微不平整 |
| 外部条件 | 负载变化、持续外力、脉冲推扰、电池电压变化 |

### 8.2 随机化实施原则

- 优先用真机测量和系统辨识确定范围；
- 先单项测试，再组合随机化；
- 区分训练分布与固定评估分布；
- 避免一开始使用过宽范围，否则策略可能过度保守；
- 记录每种随机化对跟踪误差、能耗和摔倒率的影响；
- 对一组固定“最差工况”单独评估。

RMA 通过历史信息推断隐含环境参数并快速适应未知动力学，为后续在线适应提供了进一步路线 [[8]](#ref-8)。

## 9. 第七阶段：复杂地形与外部感知

平地策略稳定并完成真机基础验证后，再依次加入：

1. 小坡度；
2. 缓坡和低台阶；
3. 随机高度场；
4. 摩擦变化地面；
5. 离散障碍和更复杂自然地形。

大规模并行训练与地形课程可参考 Rudin 等人的方法 [[3]](#ref-3)。

如果 actor 使用 `height_scan`，真机部署时必须提供等价的局部地形高度信息，例如由深度相机或激光雷达生成高度图。其他选择包括：

- 只让 critic 使用 `height_scan`，将其作为训练特权信息；
- teacher actor 使用高度信息，再蒸馏出不使用高度信息的 student；
- 完全使用本体感知历史进行盲走，适用于有限高度变化的地形 [[7]](#ref-7)。

## 10. 第八阶段：真机部署与安全验证

### 10.1 部署一致性检查

必须逐项保持一致：

```text
策略频率               50 Hz
速度指令顺序           [vx, vy, wz]
关节顺序               与训练完全一致
关节位置和速度单位     rad、rad/s
动作映射               默认角度 + 0.5 × action
动作裁剪               与训练一致
观测顺序及缩放         与训练一致
IMU 坐标系和符号        与仿真一致
```

### 10.2 建议的真机测试顺序

1. 架空测试关节方向和动作幅度；
2. 吊架或保护绳下测试站立；
3. 小范围零速度站立；
4. 低速前进和停止；
5. 低速后退、转向和横移；
6. 小推扰恢复；
7. 扩大速度范围；
8. 最后测试载荷和不同地面。

必须配置独立于策略的安全层，包括关节软硬限位、力矩/速度限制、姿态保护、通信超时、急停和失控后的安全卸力或落地逻辑。

## 11. 实验与评价方法

### 11.1 核心训练指标

- 总奖励及各奖励分量；
- 平均回合长度和各终止原因比例；
- XY 线速度跟踪误差；
- 偏航角速度跟踪误差；
- 零指令下的残余速度；
- 关节力矩、速度和动作饱和比例；
- 足端滑动速度、落地冲击和腾空时间；
- 能耗或单位运输能耗。

### 11.2 固定评估矩阵

至少分别测试：

- 站立、前进、后退、横移和原地转向；
- 低、中、高三个速度区间；
- 低摩擦和高摩擦地面；
- 质量与质心偏差；
- 不同方向和强度的推扰；
- 观测噪声与控制延迟；
- 未参与训练的参数组合。

### 11.3 实验纪律

- 固定基线、评估脚本和随机种子集合；
- 每次只修改一个主要变量；
- 重要结论尽量使用多个随机种子验证；
- 保存配置、代码版本、checkpoint、TensorBoard 日志和回放视频；
- 使用消融实验判断每个奖励或随机化是否真正有效。

## 12. 推荐实施优先级

### P0：立即执行

1. 验证机器人动力学、关节方向、足端接触和执行器限制；
2. 分离训练、回放和固定评估配置；
3. 明确动作裁剪策略并使训练与部署一致；
4. 使用简单指令和较小随机化建立可复现基线；
5. 建立奖励分量、终止原因、饱和比例和足端指标监控。

### P1：平地策略稳定后

1. 增加速度指令课程；
2. 加入关节限位、足端滑移和站立质量指标；
3. 检查 `feet_air_time` 是否造成跳跃倾向；
4. 确定真机基座线速度获取方案；
5. 引入 critic 特权观测或本体感知历史；
6. 系统化增加质量、质心、延迟和传感器偏置随机化。

### P2：基础 Sim-to-Real 成功后

1. 加入粗糙地形与地形课程；
2. 比较盲走、critic 特权信息和 teacher-student 三条路线；
3. 加入在线动力学适应；
4. 扩展踏频、步态、身体高度和抬脚高度指令；
5. 最后再研究跳跃、攀爬等高动态技能。

## 13. 当前最关键的三个决策

1. **真机是否能够稳定提供基座线速度？** 这决定 actor 是否继续使用 `base_lin_vel`。
2. **训练与真机是否采用完全相同的动作裁剪、PD 参数和控制频率？** 这决定最基础的控制接口是否一致。
3. **目标是平地遥控行走、盲走复杂地形，还是带外部感知的复杂地形？** 这决定后续观测结构、传感器配置和训练路线。

## 14. 参考文献

<a id="ref-1"></a>

[1] NVIDIA Isaac Lab Developers. [Creating a Manager-Based Base Environment](https://isaac-sim.github.io/IsaacLab/develop/source/tutorials/03_envs/create_manager_base_env.html). Isaac Lab Documentation.

<a id="ref-2"></a>

[2] Schulman, J., Wolski, F., Dhariwal, P., Radford, A., & Klimov, O. [Proximal Policy Optimization Algorithms](https://arxiv.org/abs/1707.06347). arXiv:1707.06347, 2017.

<a id="ref-3"></a>

[3] Rudin, N., Hoeller, D., Reist, P., & Hutter, M. [Learning to Walk in Minutes Using Massively Parallel Deep Reinforcement Learning](https://arxiv.org/abs/2109.11978). CoRL, 2022.

<a id="ref-4"></a>

[4] Tan, J., Zhang, T., Coumans, E., Iscen, A., Bai, Y., Hafner, D., Bohez, S., & Vanhoucke, V. [Sim-to-Real: Learning Agile Locomotion for Quadruped Robots](https://roboticsproceedings.org/rss14/p10.html). Robotics: Science and Systems, 2018. DOI: [10.15607/RSS.2018.XIV.010](https://doi.org/10.15607/RSS.2018.XIV.010).

<a id="ref-5"></a>

[5] Hwangbo, J., Lee, J., Dosovitskiy, A., Bellicoso, D., Tsounis, V., Koltun, V., & Hutter, M. [Learning Agile and Dynamic Motor Skills for Legged Robots](https://arxiv.org/abs/1901.08652). Science Robotics, 4(26), 2019. DOI: [10.1126/scirobotics.aau5872](https://doi.org/10.1126/scirobotics.aau5872).

<a id="ref-6"></a>

[6] Ji, G., Mun, J., Kim, H., & Hwangbo, J. [Concurrent Training of a Control Policy and a State Estimator for Dynamic and Robust Legged Locomotion](https://arxiv.org/abs/2202.05481). IEEE Robotics and Automation Letters, 2022.

<a id="ref-7"></a>

[7] Lee, J., Hwangbo, J., Wellhausen, L., Koltun, V., & Hutter, M. [Learning Quadrupedal Locomotion over Challenging Terrain](https://arxiv.org/abs/2010.11251). Science Robotics, 5(47), 2020.

<a id="ref-8"></a>

[8] Kumar, A., Fu, Z., Pathak, D., & Malik, J. [RMA: Rapid Motor Adaptation for Legged Robots](https://arxiv.org/abs/2107.04034). Robotics: Science and Systems, 2021.

<a id="ref-9"></a>

[9] Margolis, G. B., & Agrawal, P. [Walk These Ways: Tuning Robot Control for Generalization with Multiplicity of Behavior](https://proceedings.mlr.press/v205/margolis23a.html). Proceedings of Machine Learning Research, 205:22–31, 2023.

<a id="ref-10"></a>

[10] ETH Zürich Robotic Systems Lab. [RSL-RL: A Fast and Simple Implementation of Learning Algorithms for Robotics](https://github.com/leggedrobotics/rsl_rl).

## 15. 相关项目文档

- [BPX 环境配置说明](BPX_ENV_SETUP.md)
- [BPX 四足机器人运动改进文献指南](BPX_LOCOMOTION_LITERATURE.md)
- [训练曲线说明](RL_TRAINING_CURVES.md)
